@model NetNotes.Models.ViewNotesModel

<table class="table" id="payer">
    <thead>
        <tr>
            <th>
                @Html.DisplayName("Visit Date")
            </th>
            <th>
                @Html.DisplayName("ID")
            </th>
            <th></th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.VisitsSelectList)
        {
            <tr>

                <td>
                    @Html.DisplayFor(modelItem => item.Text)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Value)
                </td>
                <td>
                    <a href="@Url.Action("SelectedVisit","ViewNotes",
                            new { visit_id=item.Value, visitdate=item.Text })" onclick="SetVisit();">Select</a>
                </td>
            </tr>
        }
    </tbody>
</table>

@section Scripts{
    <script type="text/javascript" src="~/Scripts/DataTables/dataTables.bootstrap.js"></script>
    <script type="text/javascript" src="~/Scripts/DataTables/jquery.dataTables.js"></script>
    <script>
        $(document).ready(function () {
            $("#payer").DataTable({

            });
        });
    </script>

    <script type="text/javascript">
        function SetVisit() {
            if (window.opener != null && !window.opener.closed) {
                var txtName = window.opener.document.getElementById("searchVisit");
                txtName.value = document.getElementById('visitDate').value;
            }
            window.close();
        }
    </script>
}


